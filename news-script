const puppeteer = require('puppeteer');
const fs = require('fs');

(async () => {
  console.log('🚀 Starting automated news fetch...');
  
  try {
    // Simple test version
    const testData = {
      news: [
        {
          title: "परीक्षण समाचार 1",
          description: "यह एक परीक्षण समाचार है",
          source: "टेस्ट",
          timestamp: new Date().toISOString()
        },
        {
          title: "परीक्षण समाचार 2", 
          description: "यह दूसरा परीक्षण समाचार है",
          source: "टेस्ट",
          timestamp: new Date().toISOString()
        }
      ]
    };
    
    fs.writeFileSync('test-news.json', JSON.stringify(testData, null, 2));
    console.log('✅ Test news file created!');
    
  } catch (error) {
    console.error('Error:', error);
  }
})();